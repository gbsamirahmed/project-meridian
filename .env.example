# Optional: enables the MapTiler Satellite primary view.
# This is a browser-visible public map key; restrict it to your local and
# deployed HTTP origins in the MapTiler dashboard.
VITE_MAPTILER_KEY=
